import { SuccessResponse, ErrorResponse } from "./api";
import { FeatureType, ModelOutputType } from "./mlModel";

export { 
    SuccessResponse, 
    ErrorResponse,
    FeatureType,
    ModelOutputType,
}